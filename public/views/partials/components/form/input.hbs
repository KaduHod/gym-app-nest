<input 
    type="{{ type }}" 
    name="{{ name }}" 
    placeholder="{{ placeholder }}"
    {{#if pattern}}
        pattern={{ pattern }}
    {{/if}}
    {{#if req}}
        required="true"
    {{/if}}
    class="
        focus:outline-none
        p-1
        m-1
        rounded
    ">